# Claude-Godot Sandbox Environment Configuration
# Copy to .env and fill in your values

# =============================================================================
# CLAUDE AUTHENTICATION (choose one)
# =============================================================================

# Option 1: Claude Max subscription (recommended - included with subscription)
# Run: claude setup-token
# CLAUDE_CODE_OAUTH_TOKEN=sk-ant-...

# Option 2: API key (pay-per-use)
# Get from: https://console.anthropic.com/
# ANTHROPIC_API_KEY=sk-ant-...

# =============================================================================
# GITHUB AUTHENTICATION (choose one)
# =============================================================================

# Option 1: GitHub App (recommended - short-lived tokens, better security)
# See docs/GITHUB_APP_SETUP.md for setup instructions

# Your GitHub App ID (found in app settings: Settings > Developer settings > GitHub Apps)
# GITHUB_APP_ID=123456

# Installation ID (from the installed app URL, e.g., /installations/12345678)
# GITHUB_APP_INSTALLATION_ID=12345678

# Private key - choose ONE method:

# Method A: Base64-encoded key (for CI/CD and secure environments)
# Generate with: base64 -i ./secrets/github-app-private-key.pem | tr -d '\n'
# GITHUB_APP_PRIVATE_KEY=LS0tLS1CRUdJTiBSU0EgUF...

# Method B: Path to PEM file (for local development)
# Store the PEM file in ./secrets/ (gitignored)
# GITHUB_APP_PRIVATE_KEY_PATH=./secrets/github-app-private-key.pem

# Optional: Scope token to specific repositories (comma-separated)
# If not set, token has access to all repos where app is installed
# GITHUB_APP_REPOSITORIES=my-game,game-assets

# Option 2: Personal Access Token (legacy - less secure)
# Create at: https://github.com/settings/tokens
# GITHUB_PAT=github_pat_...

# =============================================================================
# OPTIONAL SETTINGS
# =============================================================================

# Godot version configuration
# GODOT_VERSION=4.6
# GODOT_RELEASE_TYPE=beta2

# Container resource limits
# AGENT_MEMORY_LIMIT=2G
# AGENT_CPU_LIMIT=2.0

# Queue mode settings
# QUEUE_POLL_INTERVAL=5000

# Debug output (set to 1 to enable)
# CLAUDE_DEBUG=1
# GITHUB_DEBUG=1
