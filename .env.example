# Claude-Godot Sandbox Environment Configuration
# Copy this file to .env and fill in your values:
#   cp .env.example .env

# =============================================================================
# AUTHENTICATION (Choose ONE method)
# =============================================================================
# You need EITHER an API key OR Claude Max subscription credentials.
# Run ./scripts/setup-claude-auth.sh status to check your configuration.

# -----------------------------------------------------------------------------
# Option 1: API Key (pay-per-use)
# -----------------------------------------------------------------------------
# Get your API key from: https://console.anthropic.com/
# Format: sk-ant-api03-...
# If set, this takes priority over subscription auth.
ANTHROPIC_API_KEY=

# -----------------------------------------------------------------------------
# Option 2: Claude Max Subscription (included with subscription)
# -----------------------------------------------------------------------------
# No configuration needed here! Just run:
#   ./scripts/setup-claude-auth.sh login
#
# This opens a browser for OAuth login. Credentials are stored in ~/.claude/
# and automatically mounted into containers.
#
# To override the default config path:
# CLAUDE_CONFIG_PATH=~/.claude

# =============================================================================
# Optional: Godot Configuration
# =============================================================================
# Godot version to use (default: 4.3)
# GODOT_VERSION=4.3

# Release type: stable, rc1, beta1, etc.
# GODOT_RELEASE_TYPE=stable

# SHA256 checksum for the Godot binary
# Get this from the official Godot downloads or compute it yourself
# GODOT_SHA256=

# =============================================================================
# Optional: Resource Limits
# =============================================================================
# Maximum memory for agent container (default: 2G)
# AGENT_MEMORY_LIMIT=2G

# Maximum CPU cores for agent container (default: 2.0)
# AGENT_CPU_LIMIT=2.0

# =============================================================================
# Optional: Path Configuration
# =============================================================================
# Default project path for direct mode
# PROJECT_PATH=/path/to/your/project

# Staging directory path for staging mode
# STAGING_PATH=/path/to/staging

# Live project path (for staging mode reference)
# LIVE_PROJECT_PATH=/path/to/live/project

# =============================================================================
# Optional: Claude Code Configuration
# =============================================================================
# NPM package name for Claude Code (if installing via npm)
# CLAUDE_NPM_PACKAGE=@anthropic-ai/claude-code

# Binary URL for Claude Code (alternative installation method)
# CLAUDE_BINARY_URL=
