# Claude-Godot Sandbox Environment Configuration
# Copy this file to .env and fill in your values:
#   cp .env.example .env

# =============================================================================
# AUTHENTICATION (Choose ONE method)
# =============================================================================
# You need EITHER an API key OR a Claude OAuth token.

# -----------------------------------------------------------------------------
# Option 1: Claude Subscription (recommended - included with subscription)
# -----------------------------------------------------------------------------
# Generate a long-lived OAuth token by running:
#   claude setup-token
#
# Then paste the token here:
CLAUDE_CODE_OAUTH_TOKEN=

# -----------------------------------------------------------------------------
# Option 2: API Key (pay-per-use)
# -----------------------------------------------------------------------------
# Get your API key from: https://console.anthropic.com/
# Format: sk-ant-api03-...
# Note: If both are set, API key takes priority.
ANTHROPIC_API_KEY=

# =============================================================================
# Optional: Godot Configuration
# =============================================================================
# Godot version to use (default: 4.6)
# GODOT_VERSION=4.6

# Release type: stable, beta2, beta2, rc1, etc.
# GODOT_RELEASE_TYPE=beta2

# SHA256 checksum for Godot binary verification (optional but recommended)
# NOTE: This is VERSION-SPECIFIC! Each Godot version has a different hash.
# Get the hash for your specific version:
#   curl -LO https://github.com/godotengine/godot/releases/download/4.6-beta2/Godot_v4.6-beta2_linux.x86_64.zip
#   sha256sum Godot_v4.6-beta2_linux.x86_64.zip
# GODOT_SHA256=

# =============================================================================
# Optional: Resource Limits
# =============================================================================
# Maximum memory for agent container (default: 2G)
# AGENT_MEMORY_LIMIT=2G

# Maximum CPU cores for agent container (default: 2.0)
# AGENT_CPU_LIMIT=2.0

# =============================================================================
# Optional: Path Configuration
# =============================================================================
# Default project path for direct mode
# PROJECT_PATH=/path/to/your/project

# Staging directory path for staging mode
# STAGING_PATH=/path/to/staging

# Live project path (for staging mode reference)
# LIVE_PROJECT_PATH=/path/to/live/project

# =============================================================================
# Optional: Claude Code Configuration
# =============================================================================
# NPM package name for Claude Code (if installing via npm)
# CLAUDE_NPM_PACKAGE=@anthropic-ai/claude-code

# Binary URL for Claude Code (alternative installation method)
# CLAUDE_BINARY_URL=
